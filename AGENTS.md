# x AGENTS

## 文件约定

- 运行 `just` 查看所有可用命令（Go 工具链入口：`just go ...`）
- 项目背景和包索引见 `README.md`
- 行为约束见本文档

## 开发环境

- Go ≥ 1.26（见 `go.mod`）
- 无需额外初始化，直接 `just go test`、`just go vet` 等入口

## 编码风格

- 文档、注释、错误和测试说明使用中文，专有名词除外
- 不随意新增模块、执行入口或注册关系
- 变更公共包时保持最小影响面，不破坏分层一致性
- 优先通过参数传递上下文，避免可变全局状态

## 什么时候停、什么时候问

- 需求有多种互斥实现时，停下来确认方向
- 会影响公开 API 或兼容性但没有明确迁移要求时，先确认
- 需要仓库外权限、敏感配置或发布动作时，请求人工处理
- 发现项目结构或模块分层不合理时，先指出问题再给建议
- 无法完成最小验证时，明确说明缺口

## 规则生效

- 只有落盘到 `AGENTS.md` 的规则才生效
- 子目录 `AGENTS.md` 只覆盖确有必要的局部规则，未覆盖的继承上级
